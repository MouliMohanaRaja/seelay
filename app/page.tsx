export default function Home() {
  return (
    <main style={{ maxWidth: 640, margin: "4rem auto", padding: "0 1rem" }}>
      <h1>Seelay</h1>
      <p>
        A recommendation inbox: capture trusted human recommendations (movies
        &amp; TV first) from anywhere, resolve them automatically with
        provenance intact, and find them in one trusted list when you need
        them.
      </p>
      <p>
        Walking skeleton — PLAN.md step 1.1. The receipt list, capture API,
        and resolution pipeline land in the steps that follow.
      </p>
    </main>
  );
}
