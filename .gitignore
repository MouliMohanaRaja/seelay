# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — real secrets live in .env.local, never in git.
# .env.example (placeholder names only) is deliberately committed.
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# vendored design-tooling skills — local dev aids, not product code.
# build-planner and project-setup are small, project-specific and stay tracked.
.claude/skills/emil-design-eng/
.claude/skills/frontend-design/
.claude/skills/impeccable/
